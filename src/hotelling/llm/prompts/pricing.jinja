Current market state (period {{ period }}):

Your firm: {{ firm_id }}
  - Location: ({{ location[0] | round(3) }}, {{ location[1] | round(3) }})
  - Last price index: {{ last_price_index }}  (price = {{ last_price | round(3) }})
  - Last profit: {{ last_profit | round(4) }}
  - Last demand (share): {{ last_demand | round(4) }}

Competitors:
{% for c in competitors %}
  - {{ c.firm_id }}: location ({{ c.location[0] | round(3) }}, {{ c.location[1] | round(3) }}),
    last price index {{ c.last_price_index }} (price = {{ c.last_price | round(3) }})
{% endfor %}

Price grid (index → price):
{% for i, p in price_grid %}  {{ i }}: {{ p | round(3) }}
{% endfor %}

Choose a price_index in [0, {{ m - 1 }}] to maximise your long-run profit.
Provide your chain-of-thought reasoning in the "rationale" field.
