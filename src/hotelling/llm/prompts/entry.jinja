You are an expert market strategist deciding where to open a new store and at what price.

The market is a 2-D spatial Hotelling model with logit consumer demand.
Your entry cost is {{ entry_cost | round(3) }}.

Incumbent firms already in the market:
{% for f in incumbents %}
  - {{ f.firm_id }}: location ({{ f.location[0] | round(3) }}, {{ f.location[1] | round(3) }}),
    current price {{ f.price | round(3) }}
{% endfor %}

Candidate entry locations (index → (x, y)):
{% for i, loc in candidate_locations %}
  {{ i }}: ({{ loc[0] | round(3) }}, {{ loc[1] | round(3) }})
{% endfor %}

Price grid (index → price):
{% for i, p in price_grid %}  {{ i }}: {{ p | round(3) }}
{% endfor %}

Choose:
- location_index in [0, {{ n_candidates - 1 }}]
- price_index in [0, {{ m - 1 }}]

to maximise your expected profit net of entry cost.
Provide your chain-of-thought reasoning in the "rationale" field.
Respond with a JSON object matching the EntryDecision schema.
